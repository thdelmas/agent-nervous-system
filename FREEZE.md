# Agent Nervous System — FROZEN 2026-06-30

> **This is a freeze, not a death.** A sleeping body with an alarm clock — not a corpse.
> The organs are intact, the lineage/registry stand, nothing is harvested or buried.
> **Freeze = no *expansion*, not no *use*.** The organs remain fully usable as tools
> (this freeze was itself decided using `council` + `contemplation`). What stops is
> *building new organs and expanding existing ones* — and starting any new intrinsic build.

What it is:    8 Claude Code skills as agent "organs" (consciousness-loop, octopus, rem-sleep,
               immune-check, sunset, playtime, contemplation, proprioception) + memetic-selection
               ledger (registry.json / lineage.jsonl). The "self that persists between prompts."
Why frozen:    **Deliberate attention-reallocation, not a failure.** The maintainer's
               discretionary hours are committed to a time-boxed commercial test outside this
               repo; freezing the suite removes the alibi while that thesis is tested honestly.
               Not a conviction failure — an experiment-isolation move.
Scope:         This repo (the organs + ledger), plus no new intrinsic builds elsewhere
               for the duration.

## Exemption 2026-07-14 — organism-driven self-extension

The maintainer exempted the agent's own capability requests from this freeze ("the freeze
doesn't apply for your nervous system"): when the session agent itself identifies and asks
for an organ-level capability, it may build and land it in the suite. Under this exemption:
**exteroception** (9th organ, wake-sense, forks proprioception by inversion) and the
**forecast ledger** (proprioception 0.1.0 → 0.2.0) were built and pushed 2026-07-14 —
both gated through the 2026-06-23 anti-proliferation test (operational pain + consuming
host, not metaphor-pull). The freeze otherwise stands: no maintainer-initiated expansion,
no new intrinsic builds, revival trigger unchanged.

## Revival trigger (load-bearing)

**The external commercial test resolves (expected late July 2026).** Either outcome ends
the freeze, in a different shape:
- **Thesis confirmed** → revive the infra as the *delivery engine* for the commercial line:
  internal software delivering a sold result at scale. The tunnel becomes the factory.
- **Thesis falsified** → re-decide deliberately whether the infra is an intrinsic
  end-in-itself worth the attention-rent, with eyes open.

There is nothing to exhume because nothing was buried; just resume.

## State at freeze

- 8 organs present and usable; `registry.json` / `lineage.jsonl` = the typed family tree + evidence.
- Last substantive work: registry v0.2.0 era (lineage ledger resolved 2026-06-24).
- `install.sh` / `bin/` intact — organs remain installable and runnable during the freeze.
